/* eslint-disable max-len */
// Siga as orientações do README!

const createMenu = (foods) => {
  const objectFetch = {
    fetchMenu: () => foods,
    consumption: []
  }
  return objectFetch;
};

module.exports = createMenu;
